//TODO Note commenting out as issues with Imports and taking too much time

// import { render, screen } from '@testing-library/react';
// import { useRouter } from 'next/router';
// import { useSearchParams } from 'next/navigation';
// // import { CountryDataContext } from '@/app/hooks/useCountryData';
// import { CountryDataContext } from '../../../src/app/hooks/useCountryData';
// import Country from './../../../src/app/country/page';

// // Mock the useRouter hook
// jest.mock('next/router', () => ({
//   useRouter: jest.fn(),
// }));

// // Mock the useSearchParams hook
// jest.mock('next/navigation', () => ({
//   useSearchParams: jest.fn(),
// }));


describe('Country Page', () => {
  //   beforeEach(() => {
  //     useSearchParams.mockReturnValue(new URLSearchParams('?name=Country'));
  //   });

  //   it('renders correctly with selected country data', () => {
  //     ...
  //   });

  //   it('renders "No country name provided" when countryName is missing', () => {
  //    ...
  //     expect(screen.getByText('No country name provided.')).toBeInTheDocument();
  //   });
});